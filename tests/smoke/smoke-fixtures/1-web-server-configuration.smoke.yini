/*

Features:
  - Section headers
  - Nested sections
  - Strings (single quotes)
  - Number values, booleans, nulls

*/

^ Server
host = '0.0.0.0'
port = 8080
enable_ssl = false
log_file = '/var/log/server.log'
max_connections = 200

    ^^ SSL
    enabled = false
    cert_path = null
    key_path = null
