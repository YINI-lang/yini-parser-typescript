Dependency Security (npm audit + lockfile-lint)

npm audit checks installed packages for known CVEs.

lockfile-lint ensures the lockfile only pulls from HTTPS and the official registry and has valid integrity hashes.

