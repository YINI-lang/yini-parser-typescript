# YINI Grammar (ANTLR)

This folder contains the **generated** TypeScript artifacts used by the YINI parser
(lexer, parser, visitor).  
**Do not edit files in `generated/` by hand** — re-generate them from the grammar sources.

## Regenerating the Parser Files

```bash
npm run antlr
```
